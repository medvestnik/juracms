# Install

1. Откройте `/install/`.
2. Пройдите шаги: проверка окружения, БД, создание первого super administrator, завершение.
3. Установщик записывает `config.php` и `storage/installed.lock`.
4. После установки вход: `/admin/login`.
